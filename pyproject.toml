[project]
name = "extractext"
version = "0.1.0"
description = "Extractor de texto mediante reconocimiento de caracteres."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pymupdf>=1.27.2.2",
]
